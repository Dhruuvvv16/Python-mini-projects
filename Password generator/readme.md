# Password Generator

Generate strong, random passwords with customizable length and character sets. Uses Python's `secrets` module for cryptographically secure randomness (a good companion to your Password Manager project).

## Examples

```bash
python main.py
python main.py -n 5 -l 16 --no-symbols
```
