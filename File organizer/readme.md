# File Organizer

Scan a folder (such as your Downloads directory) and automatically sort files into categorized subfolders like **Images**, **Documents**, **Videos**, **Audio**, **Archives**, **Code**, and **Others**. Includes a **dry-run mode** to preview changes before any files are moved.

## Examples

```bash
python main.py ~/Downloads --dry-run
python main.py ~/Downloads
```
