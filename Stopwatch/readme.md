# Stopwatch with Lap Tracking (CLI)

A simple command-line stopwatch controlled by pressing **Enter**. Each press records a lap, while entering **`q`** and pressing **Enter** stops the timer and displays a summary, including your fastest and slowest laps.

## Run

```bash
python main.py
```
