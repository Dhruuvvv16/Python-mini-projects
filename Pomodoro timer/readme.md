# Pomodoro Timer (CLI)

A simple command-line Pomodoro productivity timer that alternates between focused work sessions and breaks. Supports customizable work durations, short breaks, long breaks, and the number of cycles before a longer rest period.

## Examples

```bash
python main.py
python main.py --work 25 --short-break 5 --long-break 15 --cycles 4
```
