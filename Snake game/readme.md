# Snake Game

The classic Snake game built with `pygame`. Use the arrow keys to move, eat the red food to grow and score points, and avoid hitting the walls or your own tail. Press **R** to restart after a game over.

## Requirements

```bash
pip install pygame
```

## Run

```bash
python main.py
```
